node_modules
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/prisma/dev.sqlite
/prisma/dev.sqlite-journal
database.sqlite

.env
.env.*
!.env.example

# Lock other package managers if present; npm uses committed package-lock.json for CI/Docker.
yarn.lock
pnpm-lock.yaml

/extensions/*/dist

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Local tunnel / dev Partner config (copy from shopify.app.dev.toml.example)
shopify.app.dev.toml
